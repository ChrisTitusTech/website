---
import type { Post } from "../lib/content";
import PostCard from "./PostCard.astro";

interface Props { posts: Post[] }
const { posts } = Astro.props;
---
<div class="grid post-grid">{posts.map((post) => <PostCard post={post} />)}</div>
